<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dream.yzbb.wolfkiller">

    <application
        android:name=".app.DroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".ui.SettingsActivity"
            android:label="@string/main_menu_setting" />
        <activity
            android:name=".app.commonui.AboutUsActivity"
            android:label="@string/about_us" />
        <activity
            android:name=".app.commonui.DisclaimerActivity"
            android:label="@string/disclaimer" />
        <activity android:name=".ui.SetupNewGameActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CardDeliveryActivity"/>
    </application>

</manifest>
